Advance Excel Assignment 2
1. What does the dollar($) sign do?
- we use $ we we want to lock any cell or value
ex- if I want to lock A1 then $A$1- so when we want to use it will fix . IN this case we are locking A as well as 1.

2. How to Change the Reference from Relative to Absolute (or Mixed)?
- We just need to lock cell value 
ex- $A1 or $A$1 or A$1

3. Explain the order of operations in excel?
In general, Excel's order of operation follows the acronym PEMDAS (Parentheses, Exponents, Multiplication, Division, Addition, Subtraction) but with some customization to handle the formula syntax in a spreadsheet. First, any expressions in parentheses are evaluated.

4. What, according to you, are the top 5 functions in excel and write a basic syntax 
for any of two?
-There are text function like sum(), average(), today(),EOmonth(),Trim(),filter() etc
=sum(n1,n2...), =today(), =trim(text)

5. When would you use the subtotal function?
- when we want to return subtotal v;aue from range to cell, it also allow fuction like count, average. we can customize according to requirement.
- when we you subtotal and we apply filter then it will so change to subtotal also.

6. What is the syntax of the vlookup function? Explain the terms in it?
- systax-- =vlookup(lookup_value, array,column_num,[range_lookup])
lookup_value- the value which we are looking from another table
array- range of table from we are looking that value
column_num- column number in another table wher we are looking that value , ex: if we are looking roll no 1 name 
so where can write name column name in another table from where we are looking.

range_lookup- here is two option approximate match and exact match. if you choose exact match so it we look for exact or if ypu choose another one so it wiil look 
approximate.
